kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: gitea
spec:
  host: gitea.apps.openshift-sno-001.colbert.def
  to:
    kind: Service
    name: gitea-http
    weight: 100
  port:
    targetPort: http
  wildcardPolicy: None